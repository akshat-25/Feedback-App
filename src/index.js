// import React, { StrictMode } from 'react'
import React from 'react'
import ReactDOM from 'react-dom/client'  // Raect DOM library allows us to interact with the Documnet Object Model in the browser.
import './index.css'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(<App />)


